<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.expensexpert.expensexpert">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseXpert">
        <activity android:name=".ExpenseDtails"></activity>
        <activity
            android:name=".AddGroup"
            android:label="Add New Group" />
        <activity
            android:name=".AddExpense"
            android:label="Add New Expense" />
        <activity
            android:name=".AddBalance"
            android:label="Add Balance" />
        <activity
            android:name=".AddMember"
            android:label="Add New Member" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExpenseXpert.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SingleTour" />
    </application>

</manifest>